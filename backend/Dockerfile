FROM eclipse-temurin:21-jre-alpine

WORKDIR /app

# Copy the JAR generated by Maven in the pipeline
COPY target/*.jar app.jar

# Expose the port Spring Boot runs on
EXPOSE 8080

# Run the application
ENTRYPOINT ["java", "-jar", "app.jar"]